import React from 'react'
import "./index.css"

export default function Demo() {
  return (
    <div>
      <h2>Demo 内容</h2>
      <p>这是 `Demo` 组件的内容，它会在 `main-card` 区域显示，而不会刷新整个页面。</p>
    </div>
  );
}
