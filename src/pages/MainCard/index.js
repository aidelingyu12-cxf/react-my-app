import React from 'react'
import "./index.css"

export default function MainCard() {
  return (
    <div>
      <h2>MainCard 内容</h2>
      <p>这是 `MainCard` 组件的内容，它会在 `main-card` 区域显示，而不会刷新整个页面。</p>
    </div>
  );
}
